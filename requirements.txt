# Core agent stack
langgraph>=0.2.0
langchain-core>=0.3.0
anthropic>=0.34.0

# MCP — standalone RAG server + client (Map 1). Pinned: langchain-mcp-adapters
# is pre-1.0 and its surface still moves; construction is wrapped in
# agent/mcp_client.py so the future langchain.mcp (MCPAdapter) swap is one file
# (ADR-0010). fastmcp powers the server (mcp_server/server.py, ADR-0008).
mcp==1.30.0
langchain-mcp-adapters==0.3.2
fastmcp==3.4.7

# Eval experiment tracking (Map 2 E4 / R1). mlflow-skinny is the client without
# the server/UI deps; sqlalchemy+alembic give it the local SQLite tracking store
# (sqlite:///eval/mlflow.db) — offline, no running service. All MLflow use is
# wrapped in eval/tracking.py so the tracker is swappable, and record_run falls
# back to local JSON if the store is unavailable.
mlflow-skinny>=3.0
SQLAlchemy>=2.0
alembic>=1.13

# Tools
tavily-python>=0.3.0       # web search
wikipedia-api>=0.6.0       # Wikipedia lookups
arxiv>=2.1.0               # paper search
sympy>=1.13.0              # symbolic math / calculator
yfinance>=0.2.0            # analyst consensus estimates

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0       # streaming SSE responses
pydantic>=2.8.0

# RAG / vector store
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0                   # CPU-only wheel (~500MB smaller than CUDA)
sentence-transformers>=3.0.0   # all-MiniLM-L6-v2 embeddings
asyncpg>=0.29.0                # pgvector backend (async)
chromadb>=0.5.0                # embedded fallback store (ADR-0005); the MCP
                              # server's zero-external-deps CI path (ADR-0010)

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=9.0.0            # retry logic
rich>=13.0.0               # pretty CLI output
diskcache>=5.6.0           # CLI result caching (--cache flag)

# Dev / test
pytest>=9.0.0
